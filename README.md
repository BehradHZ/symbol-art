# symbol-art

Turn images into font-aware ASCII art — locally, from a terminal-style web UI or the CLI.

## Example

**Input**

<img src="examples/behrad-input.jpg" width="420" alt="Input portrait">

**Output**

<img src="examples/behrad-output.png" width="420" alt="ASCII art output">

## Run

```bash
python -m pip install -r requirements.txt
python app.py
```

Windows: `app.cmd` · CLI: `python symbol_art.py image.png -w 60`
